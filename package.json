{
  "private": true,
  "name": "@onlinemictest/guitar",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run install-bin; npm run install-hooks; npm run post-commit",
    "pre-commit": "echo '<!--svg-->' | into.js '<svg[\\s\\S]*</svg>' index.html && git add index.html",
    "post-commit": "cat guitar.svg | into.js '<!--svg-->' index.html",
    "install-hooks": "for f in `ls .git-hooks`; do ln -sf ../../.git-hooks/${f} .git/hooks/${f}; done",
    "install-bin": "for f in `ls .bin`; do ln -sf ../../.bin/${f} node_modules/.bin/${f}; done",
    "uninstall-hooks": "for f in `ls .git-hooks`; do rm .git/hooks/${f}; done",
    "uninstall-bin": "for f in `ls .bin`; do rm node_modules/.bin/${f}; done"
  },
  "author": "Florian Klampfer <mail@qwtel.com> (https://qwtel.com/)",
  "devDependencies": {
    "rollup": "^2.38.5",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-terser": "^7.0.2",
    "rollup-plugin-typescript": "^1.0.1",
    "shx": "^0.3.3",
    "tslib": "^2.1.0",
    "typescript": "^4.1.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/qwtel/onlinemictest-pitch-detector-wip.git"
  },
  "license": "AGPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/qwtel/onlinemictest-pitch-detector-wip/issues"
  },
  "homepage": "https://github.com/qwtel/onlinemictest-pitch-detector-wip#readme",
  "description": ""
}
